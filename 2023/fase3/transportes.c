#include <stdio.h>
#define T 100000
int n,m,k;
int p[T],v[T],u[T],t[T];
int a,b,ax,bx,tx;

int main(void) {
  scanf("%d %d %d",&n,&m,&k);
  for(int i=0;i<k;i++)scanf("%d",&p[i]);
  for(int i=0;i<m;i++)scanf("%d %d %d",&v[i],&u[i],&t[i]);
  scanf("%d %d",&a,&b); 

  //Impossível?  

  return 0;
}
