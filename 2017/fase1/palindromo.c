#include <stdio.h>
int palindromo(char* s,int n){
    for(int i=0;i<n;i++)
        if(s[i]!=s[n-1-i])
            return 0;
    return 1;
}
int main(void){
    int n;scanf("%d",&n);
    int s[n];scanf("%s",s);

    int x=0;
    for(int i)

    return 0;
}